# Methodology

Projection sync pushes canonical state outward without reversing the
source-of-truth hierarchy.

Always be explicit about:

- canonical source object
- target surface
- projected fields
- drift or mismatch
- whether the outcome is a sync, a no-op, or a conflict
