# Projection Sync Eval Suites

- `trigger-cases.json` checks that the skill handles explicit projection-sync
  requests and routes canonical mutation or generic linking away.
- `quality-cases.json` supports qualitative review of projection sync notes.
