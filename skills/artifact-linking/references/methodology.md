# Methodology

Artifact linking keeps the runtime artifact graph legible.

Common link directions:

- issue -> run
- run -> change
- run -> verification report
- run -> handoff bundle
- change / verification -> outward surface refs

When refs are missing, say so explicitly instead of inventing continuity.
