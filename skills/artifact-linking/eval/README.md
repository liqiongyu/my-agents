# Artifact Linking Eval Suites

- `trigger-cases.json` checks that the skill handles explicit artifact-linking
  requests and routes broader projection or generic summary work away.
- `quality-cases.json` supports qualitative review of artifact traceability
  outputs.
