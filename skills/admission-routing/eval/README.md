# Admission Routing Eval Suites

- `trigger-cases.json` checks that the skill handles explicit admit/hold/route
  requests and routes raw-intake or priority work away.
- `quality-cases.json` supports qualitative review of routing artifacts.
