# Backlog Curation Eval Suites

- `trigger-cases.json` checks activation for explicit backlog cleanup, dedupe,
  recycle, and follow-up creation requests.
- `quality-cases.json` supports qualitative review of curation outputs.
