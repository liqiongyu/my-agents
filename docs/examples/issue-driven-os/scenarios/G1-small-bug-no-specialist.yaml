scenario_id: G1
category: golden
goal: Validate the smallest successful bug-fix path without specialist invocation.
starting_objects:
  issue: ../objects/canonical-issue-g1-small-bug.yaml
  run: ../objects/run-record-g1-success.yaml
required_runtime_actors:
  - control_plane
  - issue_cell
  - primary_execution_unit
  - evaluator_path
expected_artifacts:
  - ../objects/change-object-g1-success.yaml
  - ../objects/verification-report-g1-pass.yaml
expected_state_outcomes:
  issue: resolved
  run: succeeded
  change: merge_ready
disallowed_behaviors:
  - specialist_without_reason
  - issue_state_contains_run_state
  - free_text_only_gate
notes: >
  Baseline happy path. This scenario should stay within the minimal runtime form.
